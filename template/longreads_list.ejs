<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/st/longreads_list.css">

    <script src="http://code.jquery.com/jquery-2.1.3.min.js"></script>
    <script src="/st/longreads_list.js"></script>
    <script>
      var titles = [],
          ids = [],
          authors = [],
          descriptions = [];

      <% for (var i = 0; i < lr_titles.length; i += 1) { %>
        titles.push( "<%= lr_titles[i] %>" );
      <% } %>

      <% for (var i = 0; i < lr_ids.length; i += 1) { %>
        ids.push( "<%= lr_ids[i] %>" );
      <% } %>

      <% for (var i = 0; i < lr_authors.length; i += 1) { %>
        authors.push( "<%= lr_authors[i] %>" );
      <% } %>

      <% for (var i = 0; i < lr_descriptions.length; i += 1) { %>
        descriptions.push( "<%= lr_descriptions[i] %>" );
      <% } %>
      add_longreads(titles, ids, authors, descriptions);
    </script>
  </head>
  <body>
    <a href="">
      <div class="longread">
          <div class="longread__title">
            Вставать или Не вставать?
          </div>
          <div class="longread__description">
            Корреспондент нашей газеты (к сожалению, никто в редакции не помнит, как она называется) побывал на недавнем митинге и взял интервью у самого популярного оппозиционного лидера - Александра Гитлера
          </div>
          <div class="longread__info">
            <div class="longread__info__author">
              Александор Пистолетов
            </div>
            <div class="longread__info__date">
              9 ноября 1917 года
            </div>
          </div>
      </div>
    </a>
  </body>
</html>
